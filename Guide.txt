# Create new Angular v13+ Project
ng new shopping-cart

# Add CSS Framework (ng-bootstrap)
npm i @ng-bootstrap/ng-bootstrap
ng add @ng-bootstrap/ng-bootstrap

# Add ngx-toastr for notification
npm i ngx-toastr

# Add fontawsome
npm install font-awesome --save